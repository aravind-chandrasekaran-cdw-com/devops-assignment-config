apiVersion: v1
kind: Service
metadata:
  name: sample-fastapi-app
  labels:
    app: sample-fastapi-app
spec:
  type: LoadBalancer
  selector:
    app: sample-fastapi-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000